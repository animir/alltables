<?php
/**
 * Description of ParserFactory
 *
 * @author Animir
 */
namespace Animir\Alltables;

class ParserFactory {
    static public function factory($name) {
        
        if (!is_string($name) || !array_key_exists($name, $array)) {
            
        }
    }
}
